<%= form_with(model: startform) do |form| %>
  <% if startform.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(startform.errors.count, "error") %> prohibited this startform from being saved:</h2>

      <ul>
        <% startform.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :number, style: "display: block" %>
    <%= form.number_field :number %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
